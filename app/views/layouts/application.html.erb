<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0" />
  <title><%= yield(:title) || 'Blog14' %></title>

  <% if controller.class == PostsController %>
  <%= stylesheet_link_tag "posts", media: "all", "data-turbolinks-track" => true %>
  <% else %>
  <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
  <% end %>

  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body id="<%= controller.class.name.underscore.dasherize %>">
  <div class="container-fluid">
    <div class="row">
      <div class="col-sm-12">
        <%= render 'nav' unless controller.kind_of?(SessionsController) %>
      </div>
    </div>

    <div class="row">
      <div class="col-sm-12">
        <div id="flash">
          <% flash.keys.each do |k| %>
          <div class="alert alert-<%= k %>"><%= flash[k] %></div>
          <% end %>
        </div>
      </div>
    </div>

    <div class="row"><%= yield %></div>

    <div class="row">
      <div class="col-sm-12">
        <footer>
          <p>
            Powered by
            <a href="http://rubyonrails.org/">Rails</a>
            <%= Rails::VERSION::STRING %> and
            <a href="https://www.ruby-lang.org/">Ruby</a> <%= RUBY_VERSION %>
          </p>
        </div>
      </footer>
    </div>
  </div>
</body>
</html>
